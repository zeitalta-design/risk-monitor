export const dynamic = "force-dynamic";

export const metadata = {
  title: "ウォッチ通知一覧",
  robots: { index: false, follow: false },
};

export default function WatchNotificationsLayout({ children }) {
  return children;
}
