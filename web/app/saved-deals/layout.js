export const dynamic = "force-dynamic";

export const metadata = {
  title: "保存した有望案件",
  robots: { index: false, follow: false },
};

export default function SavedDealsLayout({ children }) {
  return children;
}
